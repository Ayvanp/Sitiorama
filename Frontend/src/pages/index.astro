---
// Frontend/src/pages/index.astro
// Esta página es ahora el selector de idioma.
// No usamos el Layout principal aquí.

// Nombres de los idiomas para mostrar (puedes internacionalizarlos si quieres,
// pero para esta página simple, hardcodearlos está bien).
const languages = [
  { code: 'es', name: 'Español', href: '/es/' },
  { code: 'en', name: 'English', href: '/en/' }
];

// Puedes obtener el título del sitio de tus archivos JSON si lo deseas,
// o mantenerlo simple. Aquí usamos un título fijo.
const siteTitle = "Sitiorama"; // O el nombre de tu ciudad/proyecto
---
<!DOCTYPE html>
<html lang="es"> {/* Puedes poner 'es' o el idioma principal de tu audiencia para esta página de selección */}
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{siteTitle} - {languages.find(l=>l.code === 'es')?.name} / {languages.find(l=>l.code === 'en')?.name}</title>
  <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;700&display=swap" rel="stylesheet" />
  <style>
    body {
      font-family: 'Nunito', sans-serif;
      background-color: #0f172a; /* bg-slate-900 approx */
      color: #e2e8f0; /* text-slate-200 approx */
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      margin: 0;
      text-align: center;
    }
    .container {
      background-color: #1e293b; /* bg-slate-800 approx */
      padding: 2rem 3rem;
      border-radius: 0.75rem; /* rounded-xl */
      box-shadow: 0 10px 15px -3px rgba(0,0,0,0.1), 0 4px 6px -2px rgba(0,0,0,0.05);
    }
    h1 {
      font-size: 2.25rem; /* text-4xl approx */
      font-weight: 700;
      color: #3b82f6; /* text-blue-500 approx */
      margin-bottom: 0.5rem;
    }
    p {
      font-size: 1.125rem; /* text-lg approx */
      color: #94a3b8; /* text-slate-400 approx */
      margin-bottom: 2rem;
    }
    .language-options a {
      display: inline-block;
      background-color: #3b82f6; /* bg-blue-500 */
      color: white;
      padding: 0.75rem 1.5rem;
      margin: 0.5rem;
      border-radius: 0.375rem; /* rounded-md */
      text-decoration: none;
      font-weight: 600;
      transition: background-color 0.2s ease-in-out;
    }
    .language-options a:hover {
      background-color: #2563eb; /* bg-blue-600 */
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>{siteTitle}</h1>
    <p>Por favor, selecciona tu idioma preferido: / Please select your preferred language:</p>
    <div class="language-options">
      {languages.map(lang => (
        <a href={lang.href}>{lang.name}</a>
      ))}
    </div>
  </div>
</body>
</html>